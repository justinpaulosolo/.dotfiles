# remap prefix
unbind C-b
set-option -g prefix C-a
bind-key C-a send-prefix

# position
set-option -g status-position top

# vi mode
set-window-option -g mode-keys vi

# mouse
set -g mouse on

# index
set -g base-index 1
set -g pane-base-index 1

# split panes using v and h
unbind v
unbind h
unbind '"'
unbind %
bind v split-window -h -c "#{pane_current_path}"
bind h split-window -v -c "#{pane_current_path}"

# navigate panes using "h,j,k,l"
bind -n C-h select-pane -L
bind -n C-j select-pane -D
bind -n C-k select-pane -U
bind -n C-l select-pane -R

# reload config
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# Neovim
set-option -sg escape-time 10
set-option -g focus-events on

# Colors
set -ga terminal-overrides ',xterm-256color:Tc'

# pane border
#set -g pane-border-style fg='#323437'
#set -g pane-active-border-style fg='#949494'

# message text
#set -g message-style bg='#1e1e2e',fg='#cdd6f4'

# status line
#set -g status-style bg='#282828',fg='#EBDBB2'
#set -g status-interval 1

# status left
#set -g status-left '#[bg=#282828]#[fg=#EBDBB2]#{?client_prefix,#[fg=#eeeeee],}  󱓟  '

# window status
#set-window-option -g window-status-style fg='#EBDBB2',bg=default
#set-window-option -g window-status-current-style fg='#fb4934',bg='#282828'

# status right
#set-option -g status-right "#[fg=#8EC07C]#S #[fg=#ebdbb2]%a %d %b %I:%M %p"
